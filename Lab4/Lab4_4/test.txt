% Пример тестового файла для интерпретатора

% Инициализация переменных
A := 0;
B := 1;
C := 2;

% Операции над булевскими векторами
D := A + B;
E := B & C;
F := A -> C;
G := C <- A;
H := A ~ B;
I := A <> B;
J := A +> B;
K := A ? B;
L := A ! B;

% Поразрядное отрицание
M := \A;

% Ввод значения в переменную
read(N, 2);

% Вывод значения из переменной
write(N, 16);

% Многострочный комментарий
{
  Это многострочный комментарий
  Он занимает несколько строк
}

% Однострочный комментарий
% Это однострочный комментарий

% Дополнительные операции
O := D + E;
P := F & G;
Q := H <> I;
R := J +> K;
S := L ? M;
T := N ! O;

% Завершение тестового файла
